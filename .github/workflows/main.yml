name: Test Bot

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "SKEBOB BOT WORKS"
      - run: python3 --version
      - run: sudo apt-get update
      - run: sudo apt-get install -y ffmpeg
      - run: ffmpeg -version
